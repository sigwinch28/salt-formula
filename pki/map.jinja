{% set pki = salt['grains.filter_by']({
  'Arch': {
    'ca': {
      'crt': '/etc/ca-certificates/trust-source/anchors/ca.crt',
      'install_cmd': 'trust extract-compat',
    },
    'host': {
      'key': '/etc/ssl/private/host.key',
      'crt': '/etc/ssl/private/host.crt',
    },
  },
}, merge=salt['pillar.get']('pki:lookup')) %}
